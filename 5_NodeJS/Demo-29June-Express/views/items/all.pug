extends shared/_master

block scripts
    script(src="/static/js/all.js")

block main
    
    h1
        | All
    ul
        each item in model
            li
                a(href="/items/" + item.id) 
                    | Go to item #{item.name}
