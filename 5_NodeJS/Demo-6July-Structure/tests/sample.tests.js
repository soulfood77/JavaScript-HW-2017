const { expect } = require('chai');

describe('Test group 1', () => {
    it('testing setup', () => {
        const x = 2;
        const y = 2;

        const result = x + y;

        expect(4).to.eq(result);
    });

    it('testing setup', () => {
        const x = 2;
        const y = 2;

        const result = x + y;

        expect(4).to.eq(result);
    });
});


describe('Test group 2', () => {
    it('testing setup', () => {
        const x = 2;
        const y = 2;

        const result = x + y;

        expect(4).to.eq(result);
    });
});
